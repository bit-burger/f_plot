import 'package:flutter/material.dart';

class GraphFunctionsViewer extends StatelessWidget {
  const GraphFunctionsViewer({Key key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
