import 'package:flutter/material.dart';

class VariablesViewer extends StatelessWidget {
  const VariablesViewer({Key key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
